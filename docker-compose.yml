services:
  app:
    build: .
    env_file:
      - .env.docker
    container_name: my_app
    ports:
      - "8000:8000"
    depends_on:
      - redis


  redis:
    image: redis:7-alpine
    container_name: my_redis
    ports:
      - "6379:6379"